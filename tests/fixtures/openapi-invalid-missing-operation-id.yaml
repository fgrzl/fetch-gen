openapi: 3.0.0
info:
  title: Invalid Missing Operation ID API
  version: 1.0.0
paths:
  /broken/{id}:
    get:
      summary: This operation is missing an operationId
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
